# Market-aware session history audit fixtures

Audit tests reuse explicit offline history specs and Node33-audited packages,
then mutate isolated copies to exercise bindings and failure paths. No
credentials, network access, or real DeepSeek responses are used.
