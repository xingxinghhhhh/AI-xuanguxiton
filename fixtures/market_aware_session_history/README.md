# Market-aware session history fixtures

History tests construct explicit relative specs over isolated offline session
packages. They do not auto-discover files and require no credentials, network
access, or real DeepSeek responses.
