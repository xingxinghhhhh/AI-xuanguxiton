# Market-aware session package diff render fixtures

Renderer tests build offline Node34 diff inputs and mutate isolated copies to
verify literal rendering, failure reports, deterministic hashes, and Markdown
escaping. No credentials, network access, or real DeepSeek responses are used.
