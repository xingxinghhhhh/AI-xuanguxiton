# Market-aware session package diff fixtures

The diff tests build two independent offline session packages, update only
controlled test copies when needed, and verify literal structural changes.
They do not require credentials, network access, or real DeepSeek responses.
