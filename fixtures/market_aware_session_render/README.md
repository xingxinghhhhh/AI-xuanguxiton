# Market-aware session render fixtures

The renderer tests materialize this fixture directory's equivalent under a
temporary artifact root by replaying the existing offline market-aware session
fixture. No provider request or credential is needed.
