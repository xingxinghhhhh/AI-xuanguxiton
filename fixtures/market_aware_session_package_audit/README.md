# Market-aware session package audit fixtures

The audit tests reuse the offline five-artifact package fixture and mutate
copies to exercise independent verification. No credentials, network calls,
or real DeepSeek responses are required.
