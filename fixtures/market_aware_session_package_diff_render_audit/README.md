# Market-aware session package diff render audit fixtures

Audit tests reuse isolated offline Node34/Node35 artifacts and mutate copies
to verify SHA, path, status, and field bindings. They require no credentials,
network access, or real DeepSeek responses.
